import React from "react"
import ReactDOM from "react-dom/client"

import DiaDoProgramadorComponent from "./App.tsx"

ReactDOM.createRoot(document.getElementById("root")!).render(
	<React.StrictMode>
		<DiaDoProgramadorComponent />
	</React.StrictMode>
)
